import { Typography } from "@mui/material";

export default function NFTWarrantyYear({ warrantyYear }) {
    return (
        <Typography variant="body2" color="white">
            {warrantyYear} Years of Warranty
        </Typography>
    );
}